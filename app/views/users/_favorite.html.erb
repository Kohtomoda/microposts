<%= form_for(current_user.favorited_posts.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :favoritepost_id, @user.id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>